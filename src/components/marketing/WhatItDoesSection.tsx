export default function WhatItDoesSection() {
  return (
    <section className="px-6 py-20 sm:px-8 sm:py-28">
      <div className="mx-auto max-w-xl text-center">
        <h2 className="text-xs font-semibold uppercase tracking-[0.3em] text-muted">
          What Selfficiency does
        </h2>
        <div className="mt-8 space-y-6 text-base leading-relaxed text-ink sm:text-xl">
          <p>One thing to remove.</p>
          <p>One simplification that makes today easier.</p>
          <p>One rule to keep the weight from returning.</p>
        </div>
      </div>
    </section>
  );
}
