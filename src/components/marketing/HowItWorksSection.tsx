export default function HowItWorksSection() {
  return (
    <section className="px-6 py-20 sm:px-8 sm:py-28">
      <div className="mx-auto max-w-xl text-center">
        <h2 className="text-xs font-semibold uppercase tracking-[0.3em] text-muted">
          How it works
        </h2>
        <ol className="mt-8 space-y-5 text-base leading-relaxed text-ink sm:text-xl">
          <li>Share what feels heavy.</li>
          <li>Choose one thing to remove.</li>
          <li>Leave with a calmer next step.</li>
        </ol>
      </div>
    </section>
  );
}
